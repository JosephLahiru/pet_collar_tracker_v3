package com.example.pet_collar_tracker_v3;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;
import android.widget.Toast;

public class UserDeviceLocationHistoryMapActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_user_device_location_history_map);

        Toast.makeText(UserDeviceLocationHistoryMapActivity.this, "Map activity is still under construction !!!!", Toast.LENGTH_SHORT).show();
    }
}